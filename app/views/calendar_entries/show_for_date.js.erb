$(".cal-date").removeClass("highlight");
$(".<%= @detail_entries.first.last.first.date %>").addClass("highlight");

$("#entries").html("<%=escape_javascript(render partial: "show_for_date")%>");

$("body").scrollTop($("#entries").position().top - 20);
